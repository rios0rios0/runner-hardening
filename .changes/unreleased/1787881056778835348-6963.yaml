kind: 'Fixed'
body: 'fixed the `repo`-without-`scope` warning firing twice per host. It lived in `build_env`, which runs once to validate and once to build the bootstrap that is sent; it is a configuration check, so it now runs once in the validation pass before anything is contacted'
time: '2026-08-28T01:37:36.778805314Z'
