kind: 'Fixed'
body: 'fixed `fleet.sh` warning about a `repo` set without a `scope` on modes that never send answers, and reworded it: the key is ignored unless the host is already stored as `scope = repo`, not merely when it has no stored scope'
time: '2026-08-28T01:48:15.703836175Z'
