kind: 'Changed'
body: 'documented that an SSH agent holding more keys than `sshd`''s `MaxAuthTries` (6 by default) exhausts the attempt budget before the right key is offered, failing every host with `Too many authentication failures` even when the key is installed -- pin the identity with `IdentitiesOnly`'
time: '2026-08-28T00:42:04.807805779Z'
